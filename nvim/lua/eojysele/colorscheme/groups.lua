local colors = require("eojysele.colorscheme.colors").get_colors_from_current_themes()

local utils = require("eojysele.colorscheme.utils")

utils.set_group_settings("NormalFloat", { bg = colors.base })
